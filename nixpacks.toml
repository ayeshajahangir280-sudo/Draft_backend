[variables]
NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "python312", "python312Packages.pip"]

[phases.install]
cmds = ["npm ci", "python -m venv .venv", ".venv/bin/pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["npm run build"]
